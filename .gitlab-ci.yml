image: node:14.17.6

variables:
  HEROKU_FRONTEND_APP_NAME: doctor-today-fr
  HEROKU_BACKEND_APP_NAME: doctor-today-bk

stages:          # List of stages for jobs, and their order of execution
  - build
  - test
  - deploy

build-job:       # This job runs in the build stage, which runs first.
  stage: build
  script:
    - echo "Compiling the code..."
    - echo "Compile complete."

unit-test-job:   # This job runs in the test stage.
  stage: test    # It only starts when the job in the build stage completes successfully.
  script:
    - echo "Running unit tests... This will take about 60 seconds."
    - sleep 60
    - echo "Code coverage is 90%"

lint-test-job:   # This job also runs in the test stage.
  stage: test    # It can run at the same time as unit-test-job (in parallel).
  script:
    - echo "Linting code... This will take about 10 seconds."
    - sleep 10
    - echo "No lint issues found."

frontend-deploy:
  stage: deploy
  environment: production
  script:
    - cd frontend-user
    - npm install
    - npm run build
    - apt-get update -qq && apt-get install -y -qq ruby-dev rubygems build-essential
    - gem install dpl
    - dpl --provider=heroku --app=$doctor-today-front --api-key=$HEROKU_KEY_YC

backend-deploy:
  stage: deploy
  environment: production
  script:
    - cd backend
    - npm install
    - apt-get update -qq && apt-get install -y -qq ruby-dev rubygems build-essential
    - gem install dpl
    - dpl --provider=heroku --app=$doctor-today-back --api-key=$HEROKU_KEY_YC
